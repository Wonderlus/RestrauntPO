<UserControl x:Class="Restraunt.Controls.DeliveryAddressesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource OrangeBg}">

    <Border Background="{StaticResource CardBg}"
            BorderBrush="{StaticResource OrangeBorder}"
            BorderThickness="1"
            CornerRadius="16"
            Padding="20"
            Margin="16">

        <StackPanel>

            <TextBlock Text="Адреса доставки"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource OrangeText}"
                       Margin="0,0,0,16"/>

            <!-- Список адресов -->
            <ListBox x:Name="AddressList"
                     DisplayMemberPath="Address"
                     Height="200"
                     Margin="0,0,0,12"/>

            <!-- Новый адрес -->
            <TextBlock Text="Новый адрес" Margin="0,0,0,4"/>
            <TextBox x:Name="NewAddressTextBox"
                     Margin="0,0,0,12"/>

            <!-- Кнопки -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <Button Content="Удалить"
                        Width="100"
                        Margin="0,0,8,0"
                        Click="Remove_Click"/>

                <Button Content="Добавить"
                        Width="120"
                        Style="{StaticResource PrimaryButton}"
                        Click="Add_Click"/>
            </StackPanel>

        </StackPanel>
    </Border>
</UserControl>
